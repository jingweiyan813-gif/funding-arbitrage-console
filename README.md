# Funding Arbitrage Console

T0 project skeleton for Phase 1.

## Workspaces

- `apps/web`: React + Vite + TypeScript frontend
- `apps/server`: Node + Express + TypeScript backend
- `packages/core`: Pure TypeScript package with Vitest tests

## Commands

```bash
npm install
npm run dev
npm test
npm run build
```
